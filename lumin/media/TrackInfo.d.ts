declare module 'lumin' {

  /**
   * @enum MediaTrackType
   * @brief Media track types
   */
  enum MediaTrackType {
    kUnknown,
    kVideo,
    kAudio,
    kTimedText,
    kSubtitle,
    kMetadata,
  }
}
