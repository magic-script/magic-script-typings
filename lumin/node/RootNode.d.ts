declare module 'lumin' {

  /**
   * RootNode - The Top most Node, Every Prism instance must have a root node. This is
   * automatically created when a Prism is created, there should be no need to explicitly
   * create it.
   */
  class RootNode extends TransformNode {
  }
}
